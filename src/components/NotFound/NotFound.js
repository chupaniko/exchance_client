import React from 'react';
import PropTypes from 'prop-types';


const NotFound = () => (
  <div>
    404 Not Found
  </div>
);

NotFound.propTypes = {};

NotFound.defaultProps = {};

export default NotFound;
